---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: ubuntu
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    pre_build_image: true
    command: "sleep infinity"
provisioner:
  name: ansible
verifier:
  name: ansible
